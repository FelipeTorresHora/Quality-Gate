# Use Vite React TypeScript for Frontend

Accepted on 2026-06-20. The frontend will use Vite with React and TypeScript rather than a server-rendered framework. The dashboard is an API-backed client app, and Vite keeps the foundation lightweight while preserving typed UI code and straightforward Docker Compose development.
