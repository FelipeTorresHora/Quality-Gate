# Execute PR Webhook Analysis Automatically

Accepted on 2026-06-23. A supported GitHub App Pull Request event for a new repository, Pull Request number, and head SHA should create or reuse the Analysis Run and execute the real gate flow automatically. The first implementation may keep this synchronous, but the product behavior is automatic analysis rather than user-triggered execution of webhook-created pending runs.
