# Use pip Requirements for Backend Dependencies

Accepted on 2026-06-20. The backend will use `pip` with `requirements.txt` instead of Poetry, Hatch, or `uv`. This favors a familiar and simple dependency workflow for the foundation build while the project structure and domain model are still being established.
